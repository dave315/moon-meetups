<h1>All Meetups</h1>
<ul>
  <% @meetups.each do |meetup| %>
    <a href='/meetups/<%= meetup.id %>'><li><%= meetup.name %></li></a>
  <% end %><br>
  <a href="/new"><button>Add New Meetup</button>
</ul>
